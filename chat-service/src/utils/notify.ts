export const sendPushNotification = (receiverId: string, message: string) => {
  // Placeholder for Firebase push notification
  console.log(`Send push notification to ${receiverId}: ${message}`);
}; 